## Fork context

This repo is a fork of tiagozip/cap with production hardening on `feat/redis-cluster-mode`. Before any maintenance, merges, or releases, read **`FORK.md`** — it defines the layout rule (fork-owned additions at top level), the source deviations from upstream with intent, the release command, and the upstream-sync runbook with its merge gate.

## Agent skills

### Issue tracker

Issues live in GitHub Issues for this repo. See `docs/agents/issue-tracker.md`.

### Triage labels

Uses the default five-label vocabulary (needs-triage, needs-info, ready-for-agent, ready-for-human, wontfix). See `docs/agents/triage-labels.md`.

### Domain docs

Single-context layout — one CONTEXT.md and docs/adr/ at the repo root. See `docs/agents/domain.md`.
